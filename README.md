# 🏥 진단검사의학재단 인증심사 시스템

## 프로젝트 개요

진단검사의학재단의 인증심사 문항을 체계적으로 관리하는 웹 기반 시스템입니다. Notion 템플릿을 웹 애플리케이션으로 구현하여 더 나은 접근성과 확장성을 제공합니다.

- **목표**: 인증심사 문항의 효율적 관리 및 개정 이력 추적
- **주요 기능**: 
  - 150개 심사문항 관리
  - 다차원 필터링 (년도, 유형, 분야, 상태, 우선순위)
  - 4가지 뷰 모드 (대시보드, 테이블, 칸반, 갤러리)
  - 개정 이력 추적 및 댓글 협업 기능
  - 실시간 통계 대시보드

## 📱 URL

- **개발 서버**: https://3000-ih9hmg1bnyqudr3ynd88v-cc2fbc16.sandbox.novita.ai
- **GitHub**: (배포 예정)
- **프로덕션**: (Cloudflare Pages 배포 예정)

## 🏗️ 데이터 아키텍처

### 데이터 모델

#### 1. **분야 분류** (categories)
```
- 조직 및 인력
- 시설 및 환경
- 장비 및 기기
- 검사 전 과정
- 검사 수행
- 검사 후 과정
- 정도관리
- 정보시스템
- 안전관리
- 연구개발
```

#### 2. **심사문항 마스터** (assessment_items)
```
- 문항번호 (예: 01.010.020)
- 년도 (2023-2025)
- 유형 (핵심C, 필수R, 기본B)
- 분야, 제목, 내용
- 배점방식 (점검표, 등급제, 4단계, 2단계)
- 상태 (🟢 사용중, 🟡 검토중, 🔴 폐기됨)
- 우선순위 (높음, 보통, 낮음)
- 담당자 배정
```

#### 3. **개정 이력** (revision_history)
```
- 개정 유형 (신규, 수정, 삭제, 통합, 분리)
- 개정일, 시행일
- 변경 내용 및 사유
- 변경자 정보
```

#### 4. **담당자 관리** (managers)
```
- 이름, 소속, 전문분야
- 이메일, 전화번호
- 권한 (관리자, 심사위원, 검토자, 뷰어)
```

#### 5. **댓글** (comments)
```
- 문항별 댓글 및 답글
- 작성자 정보
- 작성 시간
```

#### 6. **첨부파일** (attachments)
```
- 문항별 첨부파일 관리
- 파일 메타데이터
- 업로드자 정보
```

### 스토리지 서비스

- **Cloudflare D1 Database**: SQLite 기반 관계형 데이터베이스
  - 로컬 개발: `--local` 모드 사용
  - 프로덕션: Cloudflare 글로벌 분산 D1
- **데이터 구조**: 정규화된 관계형 모델
- **데이터 플로우**: 
  ```
  프론트엔드 (Alpine.js) 
    ↓ Axios HTTP 요청
  Hono API (REST)
    ↓ D1 Prepared Statements
  Cloudflare D1 Database
  ```

## 🎨 주요 기능

### 1. **대시보드 뷰** 📊
- 전체 문항 수 통계
- 유형별 분류 (핵심C, 필수R, 기본B)
- 최근 개정 이력 10개 표시
- 실시간 데이터 업데이트

### 2. **테이블 뷰** 📋
- 전체 문항 목록 조회
- 5가지 필터링 기능:
  - 검색 (문항번호, 제목, 내용)
  - 년도별 (2023-2025)
  - 유형별 (핵심C, 필수R, 기본B)
  - 상태별 (사용중, 검토중, 폐기됨)
  - 우선순위별 (높음, 보통, 낮음)
- 문항 클릭 시 상세 정보 모달

### 3. **칸반 보드 뷰** 🗂️
- 유형별 3개 컬럼 (핵심C, 필수R, 기본B)
- 드래그 앤 드롭 (향후 구현 예정)
- 각 카드에 우선순위 표시
- 스크롤 가능한 컬럼

### 4. **갤러리 뷰** 🖼️
- 2025년 신규/수정 문항 집중 표시
- 그리드 레이아웃
- 태그 시각화
- 시각적 아이콘

### 5. **문항 상세 정보** 🔍
- 기본 정보 (문항번호, 년도, 유형, 분야)
- 제목 및 내용
- 개정 이력 타임라인
- 댓글 및 협업 기록
- 첨부파일 목록

## 📡 API 엔드포인트

### 분야 분류
- `GET /api/categories` - 전체 분야 목록
- `GET /api/categories/:id` - 분야 상세

### 담당자 관리
- `GET /api/managers` - 전체 담당자 목록
- `GET /api/managers/:id` - 담당자 상세

### 심사문항
- `GET /api/items` - 문항 목록 (필터링 지원)
  - Query Parameters: `search`, `year`, `type`, `category_id`, `status`, `priority`
- `GET /api/items/:id` - 문항 상세 (개정이력, 댓글 포함)
- `POST /api/items` - 문항 생성
- `PUT /api/items/:id` - 문항 수정
- `DELETE /api/items/:id` - 문항 삭제

### 개정 이력
- `GET /api/revisions` - 전체 개정 이력
  - Query Parameters: `year`, `item_id`, `revision_type`
- `POST /api/revisions` - 개정 이력 추가

### 댓글
- `POST /api/comments` - 댓글 추가
- `DELETE /api/comments/:id` - 댓글 삭제

### 통계
- `GET /api/stats` - 대시보드 통계 데이터

## 🛠️ 기술 스택

- **Frontend**: 
  - HTML5 + Tailwind CSS
  - Alpine.js (반응형 UI)
  - Axios (HTTP 클라이언트)
  - Font Awesome (아이콘)

- **Backend**: 
  - Hono Framework (v4.11.4)
  - TypeScript
  - Cloudflare Workers Runtime

- **Database**: 
  - Cloudflare D1 (SQLite)
  - Prepared Statements (SQL Injection 방지)

- **Deployment**: 
  - Cloudflare Pages
  - Wrangler CLI

## 🚀 로컬 개발 가이드

### 1. 사전 요구사항
```bash
Node.js >= 18
npm >= 9
```

### 2. 프로젝트 클론 및 설치
```bash
git clone <repository-url>
cd webapp
npm install
```

### 3. D1 데이터베이스 초기화
```bash
# 마이그레이션 실행
npm run db:migrate:local

# 샘플 데이터 삽입
npm run db:seed
```

### 4. 개발 서버 시작
```bash
# 빌드
npm run build

# PM2로 서버 시작
npm run clean-port  # 포트 3000 정리
pm2 start ecosystem.config.cjs

# 또는 직접 실행 (개발 모드)
npm run dev:sandbox
```

### 5. 접속
```
브라우저: http://localhost:3000
API 테스트: curl http://localhost:3000/api/stats
```

### 6. PM2 관리
```bash
pm2 list                     # 서비스 목록
pm2 logs webapp --nostream   # 로그 확인
pm2 restart webapp           # 재시작
pm2 delete webapp            # 중지 및 제거
```

## 📊 데이터베이스 관리

### 마이그레이션
```bash
# 로컬 환경
npm run db:migrate:local

# 프로덕션 환경
npm run db:migrate:prod
```

### 데이터베이스 초기화
```bash
# 데이터베이스 리셋 (로컬만)
npm run db:reset
```

### 콘솔 접속
```bash
# 로컬 데이터베이스
npm run db:console:local

# 프로덕션 데이터베이스
npm run db:console:prod
```

### SQL 쿼리 실행
```bash
# 파일로 실행
wrangler d1 execute webapp-production --local --file=./query.sql

# 직접 실행
wrangler d1 execute webapp-production --local --command="SELECT * FROM categories"
```

## 📦 배포 가이드

### Cloudflare Pages 배포

#### 1. D1 데이터베이스 생성
```bash
# 프로덕션 데이터베이스 생성
npx wrangler d1 create webapp-production

# database_id를 wrangler.jsonc에 업데이트
```

#### 2. 마이그레이션 적용
```bash
npm run db:migrate:prod
```

#### 3. Pages 프로젝트 생성 및 배포
```bash
# 빌드
npm run build

# Pages 프로젝트 생성
npx wrangler pages project create webapp --production-branch main

# 배포
npm run deploy:prod
```

#### 4. 환경 변수 설정 (필요시)
```bash
npx wrangler pages secret put API_KEY --project-name webapp
```

## 📝 사용 가이드

### 문항 검색
1. 테이블 뷰 선택
2. 검색창에 문항번호 또는 제목 입력
3. 필터 적용 (년도, 유형, 상태 등)

### 문항 상세 보기
1. 테이블, 칸반, 갤러리 뷰에서 문항 클릭
2. 모달에서 상세 정보 확인
3. 개정 이력 및 댓글 확인

### 2025 개정사항 확인
1. 갤러리 뷰 선택
2. 신규/수정 태그가 있는 문항만 표시됨

### 통계 확인
1. 대시보드 뷰 선택
2. 전체 통계 및 최근 개정 이력 확인

## ✅ 현재 구현 완료 기능

- ✅ D1 데이터베이스 스키마 (6개 테이블)
- ✅ 샘플 데이터 (30개 문항, 23개 개정이력, 7명 담당자, 7개 댓글)
- ✅ REST API 엔드포인트 (CRUD 완료)
- ✅ 4가지 뷰 구현 (대시보드, 테이블, 칸반, 갤러리)
- ✅ 다중 필터링 기능
- ✅ 문항 상세 모달
- ✅ 반응형 UI (모바일 대응)
- ✅ 로컬 개발 환경 (PM2 + Wrangler)

## 🔜 향후 개발 계획

### Phase 2: 고급 기능
- [ ] 문항 생성/수정/삭제 UI
- [ ] 개정 이력 추가 UI
- [ ] 댓글 작성 및 삭제 UI
- [ ] 칸반 보드 드래그 앤 드롭
- [ ] 첨부파일 업로드 (R2 Storage 연동)

### Phase 3: 협업 기능
- [ ] 사용자 인증 (Clerk/Auth0)
- [ ] 권한 관리 (RBAC)
- [ ] 실시간 알림
- [ ] 이메일 알림 (SendGrid)

### Phase 4: 분석 및 리포팅
- [ ] 차트 및 그래프 (Chart.js)
- [ ] Excel/PDF 내보내기
- [ ] 년도별 비교 분석
- [ ] 개정 이력 통계

### Phase 5: AI 기능 (선택)
- [ ] 문항 자동 분류
- [ ] 유사 문항 추천
- [ ] 개정 사유 자동 요약

## 🤝 기여 가이드

1. 이슈 생성
2. 브랜치 생성 (`feature/기능명`)
3. 커밋 작성 (Conventional Commits)
4. Pull Request 제출

## 📄 라이선스

이 프로젝트는 진단검사의학재단의 내부 시스템으로, 비공개 프로젝트입니다.

## 📞 지원

- **개발자**: AI Assistant
- **이슈 트래킹**: GitHub Issues
- **문의**: 프로젝트 관리자

---

## 🎯 현재 상태

- **배포 상태**: ✅ 로컬 개발 완료
- **마지막 업데이트**: 2026-01-18
- **버전**: 1.0.0
- **데이터**: 30개 샘플 문항 (2023-2025)

**프로젝트는 정상적으로 작동하며, Cloudflare Pages 배포 준비가 완료되었습니다.**
